import { ethers } from "ethers";
import sdk from "./1-initialize-sdk.js";

// airdrop governance tokens to DAO members

// This is the address to our ERC-1155 membership NFT contract aka "Bundle Drop" under Module Address in Dashboard
const bundleDropModule = sdk.getBundleDropModule(
  "0xF0222Db89f1ad94696eab5171C6c0870722cee31",
);

// This is the address to our ERC-20 token contract aka token module (step 5)
const tokenModule = sdk.getTokenModule(
  "0x4444a130b72Fc53f041D9d290061C3C2EC21AC8b",
);

(async () => {
  try {
    // Grab all the addresses of people who own our membership NFT, which has 
    // a tokenId of 0.
    const walletAddresses = await bundleDropModule.getAllClaimerAddresses("0");
  
    if (walletAddresses.length === 0) {
      console.log(
        "No NFTs have been claimed yet, maybe get some friends to claim your free NFTs!",
      );
      process.exit(0);
    }
    
    // Loop through the array of addresses.
    const airdropTargets = walletAddresses.map((address) => {
      // Pick a random # between 1000 and 10000.
      const randomAmount = Math.floor(Math.random() * (10000 - 1000 + 1) + 1000);
      console.log("✅ Going to airdrop", randomAmount, "tokens to", address);
      
      // Set up the target.
      const airdropTarget = {
        address,
        // Remember, we need 18 decimal placees!
        amount: ethers.utils.parseUnits(randomAmount.toString(), 18),
      };
  
      return airdropTarget;
    });
    
    // Call transferBatch on all our airdrop targets.
    console.log("🌈 Starting airdrop...")
    await tokenModule.transferBatch(airdropTargets);
    console.log("✅ Successfully airdropped tokens to all the holders of the NFT!");
  } catch (err) {
    console.error("Failed to airdrop tokens", err);
  }
})();